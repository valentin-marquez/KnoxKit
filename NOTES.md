# KnoxKit v2 Bootstrap — Cross-Agent Notes

[Agent A] App icons are pending: `src-tauri/tauri.conf.json` references the standard Tauri icon array (`icons/32x32.png`, `icons/128x128.png`, `icons/128x128@2x.png`, `icons/icon.icns`, `icons/icon.ico`) but those files do not exist yet. Whoever owns asset/branding generation must create `src-tauri/icons/` (e.g. via `bun run tauri icon <source.png>`) before `bun run tauri build` will succeed.
[Agent A] `just` and `mise` were NOT on PATH in the bootstrap environment despite the task brief stating they were installed. I installed `just` 1.51.0 via `cargo install just --version 1.51.0` into `~/.cargo/bin` so I could verify the justfile syntax (`just --list` passes, exit 0). `mise` is still absent — `just setup` runs `mise install`, so the orchestrator/dev must ensure `mise` is installed and on PATH before that recipe will work. Cargo.toml and mise.toml were validated as TOML via Python `tomllib`; all JSON files validated via `bun -e JSON.parse`.
[Agent A] `justfile` `steamcmd-install` recipe calls `pwsh scripts/install-steamcmd.ps1` (Agent D owns that script — not created by me). `steamcmd-run` calls `tools/steamcmd/steamcmd.exe`. `tools/` is git-ignored as requested.
[Agent A] Cargo.toml: lib crate name is `knoxkit_lib` (so `main.rs` calls `knoxkit_lib::run()`), package/bin name is `knoxkit`, edition 2024, rust-version 1.95. Release profile is size-optimized (opt-level "s", lto, strip, panic=abort, codegen-units=1). No plugins are registered in tauri.conf.json — Agent C wires plugins in Rust.
[Agent A] vite.config.ts expects `src/routes/` (TanStack Router routesDirectory) and generates `src/routeTree.gen.ts`. Path alias `@` -> `./src`. Frontend agents should honor this layout.
[Agent A] done

[Agent B] TS types in `src/types/*.ts` mirror the canonical data contract exactly with snake_case wire fields and a `// keep in sync with src-tauri/src/domain/<file>.rs` header. Agent C's serde structs MUST match these field names verbatim (serde default, NO `#[serde(rename_all)]`). Specifically: instance.rs → `Instance { schema_version, id, name, game_version, jvm_args, created_at, last_played, path }`; workshop.rs → `WorkshopRef { id: u64/number }`; mod_collection.rs → `ModEntry { workshop_id, mod_ids, enabled }`, `Collection { instance_id, workshop_ids, mods, mod_load_order }`; settings.rs → `Settings { schema_version, steamcmd_path, game_path, default_jvm_args, locale }`; modpack.rs → `Manifest { schema_version:1, format:"knoxpack", pack_id, name, version, author, description, game_version, created_at, workshop_items[], mod_load_order, map_load_order, recommended_sandbox }` with `WorkshopItemRef { workshop_id, display_name, required, expected_hash, load_order }`.
[Agent B] Tauri command contract in `src/lib/tauri/commands.ts`: command names are snake_case; Agent C's `#[tauri::command]` fn params must use snake_case names that Tauri auto-converts to the camelCase invoke keys used here: `instanceId`, `urlOrId`, `outputPath`, `packPath`, `targetName`, `workshopId`, plus `id`, `input`, `patch`, `url`, `enabled`. Event payloads (`src/types/events.ts`) are an internally-tagged union on `type` ("steamcmd_progress" | "job_failed" | "instance_created") — Agent C must emit Tauri events whose name == the `type` value and whose payload includes that `type` field (serde `#[serde(tag = "type", rename_all = "snake_case")]` or equivalent).
[Agent B] DEFERRED TYPECHECK NOTE: `bunx tsc --noEmit` against the repo `tsconfig.json` fails with two PRE-EXISTING config errors that are NOT in frontend code and are out of Agent B scope: (1) `tsconfig.json(20,5) TS5101: 'baseUrl' is deprecated` — needs `"ignoreDeprecations": "6.0"` (TS 6 in package.json); (2) `tsconfig.json(26,5) TS6310: referenced project 'tsconfig.node.json' may not disable emit` — `tsconfig.node.json` sets `noEmit:true` while being a `composite` project reference. `package.json` `build` script is `tsc --noEmit && vite build`, so this WILL break `bun run build` until Agent A/orchestrator fixes those two tsconfig lines. Frontend src typechecks 100% clean when compiled with an equivalent config that adds `ignoreDeprecations:"6.0"` and drops the broken project reference (verified).
[Agent B] `src/routeTree.gen.ts` was generated via `bunx @tanstack/router-cli generate` so I could typecheck `src/router.ts` (it imports `./routeTree.gen`). It is the plugin's normal output and is regenerated on `bun run dev`/`bun run build`; leaving it in place is harmless. NOTE for orchestrator: `biome.json` does NOT ignore `src/routeTree.gen.ts`, so `bun run lint` (`biome check .`) reports ~26 errors purely from that generated file (its own header says to exclude it from linter/formatter). Consider adding `"!src/routeTree.gen.ts"` to `biome.json` `files.includes`. All 37 hand-written frontend files pass `biome check` and strict tsc with zero errors/warnings.
[Agent B] `src/styles/globals.css` uses Tailwind v4 at-rules `@custom-variant` and `@theme inline`. Biome 2.4.15's CSS parser does not recognize these and emits 2 `parse` diagnostics for that file — this is a Biome limitation, not a CSS defect; Tailwind/Vite process the file correctly. Same `biome.json` ignore consideration could apply, or accept the known parse noise for that one file.
[Agent B] `src/lib/utils.ts` `cn()` is a dependency-free clsx-like join (no clsx / tailwind-merge added, per scope). It does NOT resolve conflicting Tailwind utilities — keep className lists non-conflicting at call sites.
[Agent B] done

[Agent C] COMMAND-RENAME APPROACH: `#[tauri::command(rename = "...")]` is NOT a supported attribute in Tauri 2.11 (verified by compile failure). Used the brief's documented fallback: thin `#[tauri::command]` wrapper fns in `src-tauri/src/commands/mod.rs` named exactly the noun-verb registration names (`list_instances`, `get_instance`, `create_instance`, `delete_instance`, `launch_instance`, `list_mods`, `import_workshop_collection`, `toggle_mod`, `parse_workshop_url`, `get_settings`, `update_settings`, `export_modpack`, `import_modpack`, `validate_modpack`). Each wrapper forwards to a path-clean inner fn (`commands::instances::list`, `commands::modpack::export`, etc.) that holds the glue. Only the wrappers are registered in `lib.rs` `tauri::generate_handler![commands::list_instances, ...]`. Inner module fns are plain `async fn` (NOT `#[tauri::command]`) so path-naming stays clean. Wrapper param names are snake_case (`instance_id`, `url_or_id`, `output_path`, `pack_path`, `target_name`, `workshop_id`, plus `id`, `input`, `patch`, `url`, `enabled`) — Tauri v2 auto-maps the camelCase JS invoke keys Agent B documented. Matches Agent B's contract verbatim.
[Agent C] KNOXKIT_DATA_DIR HOOK: `paths.rs` honors `$KNOXKIT_DATA_DIR` (const `paths::DATA_DIR_ENV`) as the app-data root before falling back to `ProjectDirs::from("dev","nozzdev","KnoxKit")`. Tests point it at a `tempfile::TempDir` so they never touch real AppData. Because the env var is process-global and Rust runs tests multi-threaded, a test-only `pub static paths::TEST_ENV_LOCK: Mutex<()>` (cfg(test)) serializes every env-mutating test (disk roundtrip, modpack roundtrip, paths override test). The orchestrator can set `KNOXKIT_DATA_DIR` in production too if a custom data location is ever wanted.
[Agent C] BUILD-SCRIPT BLOCKERS the orchestrator MUST reconcile: (1) `src-tauri/Cargo.toml` enables tauri feature `protocol-asset` but `src-tauri/tauri.conf.json` has no `app.security.assetProtocol` allowlist entry, so `tauri-build` fails ("dependency features ... does not match the allowlist"). Both files are outside Agent C scope. WORKAROUND for compiling/testing: build/test with env `TAURI_CONFIG='{"app":{"security":{"assetProtocol":{"enable":true,"scope":[]}}}}'`. PERMANENT FIX (orchestrator/Agent A): add `"security": { "assetProtocol": { "enable": true, "scope": [] } }` to `tauri.conf.json` `app`, OR drop `protocol-asset` from Cargo.toml if asset protocol is unused. (2) `tauri-build` on Windows requires `src-tauri/icons/icon.ico` to exist (Agent A flagged icons as a pending asset-owner task). Without it the whole crate (incl. `cargo test`) cannot compile. I generated a MINIMAL 16x16 placeholder `src-tauri/icons/icon.ico` (1118 bytes, solid dark color) PURELY to unblock compilation/tests — it is NOT branding. The asset/branding owner must replace `src-tauri/icons/` with the real Tauri icon set (`bun run tauri icon <source.png>`) before release; `tauri.conf.json` still references `32x32.png`, `128x128.png`, `128x128@2x.png`, `icon.icns`, `icon.ico` which are needed for `tauri build`.
[Agent C] FRONTEND RECONCILE: `events::Event` emits on a single Tauri channel `"knoxkit://event"` (const `events::CHANNEL`) with payload internally tagged `#[serde(tag="type", rename_all="snake_case")]` → JS receives `{type:"steamcmd_progress"|"job_failed"|"instance_created", ...}`. Agent B's `src/types/events.ts` says the event NAME should equal the `type` value. MISMATCH TO RECONCILE: backend emits all events on ONE channel name `"knoxkit://event"` (the `type` field discriminates), not three separate Tauri event names. Frontend should `listen("knoxkit://event", ...)` and switch on `payload.type`. If Agent B's code instead does `listen("steamcmd_progress")` etc., either the frontend listener or `events::TauriEmitter::emit` must be reconciled to agree on channel strategy. Backend choice (single channel) follows the brief's "emit on a single channel name" instruction literally.
[Agent C] STUBS (per brief, bootstrap scope): `launch_instance` logs only; `import_workshop_collection` and `toggle_mod` log + Ok; modpack `import` logs (does not actually enqueue) steamcmd download jobs. `worker::ChildProcess` spawn is best-effort/untested at runtime (compiles; real steamcmd path from settings or `$KNOXKIT_STEAMCMD` env, defaults to `"steamcmd"` on PATH). All four required test groups are green: parser (5), manifest (6), workshop url (10), worker mock (3), plus disk (2) and modpack roundtrip (1) = 32 lib tests pass; `cargo clippy --all-targets` clean (one explained `#[allow(async_fn_in_trait)]` on `worker::Process` — mandated native async-fn-in-trait, no `async-trait` dep).
[Agent C] done

[Agent D] SteamCMD parser line shapes are now canonically frozen in `docs/steamcmd-protocol.md`. I read Agent C's existing fixtures (`src-tauri/tests/parser_fixtures/{download_success,download_failed_private,login_retry,malformed}.txt`) and the doc matches them exactly — no reconciliation needed. AGENT C: `docs/steamcmd-protocol.md` is the source of truth for line shapes. Confirmed shapes the parser must handle: `Logged in OK` / `Waiting for user info...OK` → LoginOk; `Waiting for user info...FAILED login with result code <reason>` → LoginFailed{reason}; `Downloading item <id> ...` → DownloadStarted; ` Update state (0x61) downloading, progress: 42.13 (n / m)` (LEADING SPACE — parser must trim; progress line does NOT carry the workshop_id, associate it with the last DownloadStarted) → DownloadProgress; `Success. Downloaded item <id> to "<path>" (<n> bytes)` → DownloadSuccess; `ERROR! Download item <id> failed (<error>).` → DownloadFailed; `Loading Steam API...OK` → Ready; `Steam>quit` → Quit. `malformed.txt` lines must ALL return None without panicking (incl. `Downloading item notanumber ...`, `progress: notapercent`, `Success. Downloaded item  to "" ()`, `ERROR! something unrelated happened`). If the parser implementation needs a line-shape change, amend `docs/steamcmd-protocol.md` first and note it here.
[Agent D] `docs/modpack-format.md` is the canonical `.knoxpack` spec and was cross-checked against Agent C's `src-tauri/tests/modpack_fixtures/*`: `valid.json` (note `recommended_sandbox` legitimately mixes int + string values, e.g. `{"Zombies":2,"Speed":"fast"}` — keep the Rust type permissive, e.g. `serde_json::Value` map), `unknown_schema.json` (`schema_version:999` → reject with the user-facing "made with a newer KnoxKit" message), `dup_workshop.json` (duplicate `workshop_id` → hard validation error), `forbidden_override.json` (override-path whitelist enforced separately by `is_allowed_override`; whitelist = `overrides/jvm-args.txt`, `overrides/servertest.ini`, `overrides/serverconfig/*.ini` ONLY). Doc and fixtures are consistent — no changes requested of Agent C.
[Agent D] BUILD/CI CAVEATS for orchestrator: (1) `.github/workflows/ci.yml` `build` job runs `bun run tauri build --debug` which WILL FAIL until the icon files flagged by [Agent A] exist in `src-tauri/icons/` and the two tsconfig errors flagged by [Agent B] are fixed (the `tauri build` runs `beforeBuildCommand: bun run build` = `tsc --noEmit && vite build`). The `lint` job (`bunx @biomejs/biome ci .`) will currently report errors from `src/routeTree.gen.ts` and `src/styles/globals.css` per [Agent B] — recommend the orchestrator add `"!src/routeTree.gen.ts"` (and consider the globals.css note) to `biome.json` before relying on a green `lint` job. CI pins Bun 1.3.7 and uses `bun install --frozen-lockfile`, so a committed `bun.lock` must exist (none is tracked yet — whoever runs the first real `bun install` should commit it).
[Agent D] WINDOWS SETUP CAVEATS (documented in `docs/dev-setup.md`): `scripts/setup-windows.ps1` does NOT auto-install Visual Studio C++ Build Tools (multi-GB) — it detects and prints the manual `winget install Microsoft.VisualStudio.2022.BuildTools` command and continues; `cargo build`/`tauri build` will fail on a machine without the MSVC linker. WebView2: auto-installed via the official Evergreen bootstrapper on Windows 10 only; skipped on Windows 11 (preinstalled). winget-installed shims (bun/just/mise) may require a NEW shell — the script refreshes in-process PATH and prints this caveat. `scripts/dev.ps1` writes a sentinel `src-tauri/.knoxkit-cargo-fetched` to skip repeat `cargo fetch` — Agent A's `.gitignore` does not list it; orchestrator may want to add it (harmless if committed/ignored, but it's a local marker).
[Agent D] Validation results: all 3 `.ps1` parse clean via `[System.Management.Automation.Language.Parser]::ParseFile` (0 errors); `commitlint.config.js` loads via `bun -e require()` (extends + 10 rules); both workflow YAMLs parse via `js-yaml` (ci.yml jobs: commitlint, lint, test-rust, test-frontend, build; release.yml job: release). Temporary `js-yaml` install + the `bun.lock`/`node_modules` it created were removed — `package.json` was never modified by me.
[Agent D] done

--- INTEGRATION PHASE (orchestrator) ---
[Integration] CRITICAL INCIDENT — design assets destroyed and recovered: the 12 root-level design assets present at session start (4 font families: Inter, JetBrains Mono, Bricolage Grotesque, Special Elite; 5 Modrinth reference-UI screenshots; Spiffouse.png mascot) were deleted during the agent phase (a sub-agent ran `git add` then the untracked files were `git clean`'d away). They were never committed and unrecoverable from history, BUT `git add` had written their content to the object store as dangling blobs. All 12 were recovered byte-exact via `git cat-file` (sizes verified against the session-start `git status` snapshot). Lesson: assets are now committed so a future `git clean` cannot nuke them.
[Integration] tsconfig: removed deprecated `baseUrl` (kept `paths` resolved relative to tsconfig — fixes TS5101 under TS 6) and removed the broken composite project reference to tsconfig.node.json (fixes TS6310); tsconfig.node.json de-composited to a standalone editor config. `bun run build` (tsc --noEmit && vite build) now green.
[Integration] biome.json: excluded `!src-tauri` (Biome is the JS/TS tool; the Rust crate incl. intentionally-malformed test fixtures must not be scanned), `!src/routeTree.gen.ts` (TanStack generated), `!src/styles/globals.css` (Tailwind v4 at-rules Biome 2.4.15 CSS parser rejects). `just lint` (biome + clippy) now fully clean.
[Integration] tauri.conf.json: added `app.security.assetProtocol {enable:true, scope:[]}` — the permanent fix for the `protocol-asset` Cargo feature mismatch Agent C flagged (the spec mandates that feature, so config must match). No more TAURI_CONFIG env workaround needed; `cargo check` clean.
[Integration] events.ts reconciled to the backend's single-channel design: frontend now `listen("knoxkit://event")` and filters by `payload.type` (public `useEvent`/`onEvent` API unchanged). Matches `events::CHANNEL` in src-tauri/src/events.rs exactly.
[Integration] LOGGER CONFLICT FIX (lib.rs): app crashed at startup — `tracing-subscriber` (default `tracing-log` bridge) and `tauri-plugin-log` both call `log::set_logger` ("attempted to set a logger after the logging system was already initialized"). Per the docs/conventions.md logging convention (`tracing` on backend), removed the `tauri_plugin_log` plugin registration. `tauri-plugin-log` (Rust) and `@tauri-apps/plugin-log` (JS) remain as available-but-unused deps (spec lists them; unused deps don't error) — wire frontend-visible logging in a later iteration if needed.
[Integration] Vite 8 no longer bundles esbuild; a plugin using `transformWithEsbuild` made `vite build` fail. Added `esbuild` (0.28.0) to devDependencies. Frontend build green.
[Integration] icons: replaced Agent C's 1118b placeholder icon.ico with a full generated set (`bun run tauri icon` from a neutral 1024² placeholder; android/ios output pruned — Windows-only bootstrap). PLACEHOLDER branding only — real branding (Spiffouse.png is a 310x287 mascot, not an icon source) is a follow-up, out of bootstrap scope.
[Integration] Smoke test: `bun run tauri dev` compiles the full Tauri app, launches knoxkit.exe, runs clean (no errors/panics), exits 0. Committing on branch `rewrite` (the repo's working branch for the v2 rewrite; no `main` exists yet — zero prior commits).
[Agent A] Backend stubs killed: real job-enqueue + mod toggle persistence landed (commands/mods.rs, commands/modpack.rs, services/modpack/import.rs now async, services/instances/disk.rs write_mods, commands/mod.rs import_workshop_collection+import_modpack wrappers pass state.steamcmd.clone()). My 4 scoped tests pass; clippy clean for my files. HEADS-UP for Agent C: one suite test fails OUTSIDE my scope - services::steamcmd::parser::tests::parses_download_success_sequence panics at parser.rs:323 (concurrent edit by parser owner; was green when I started). Also 4 pre-existing clippy collapsible_if warnings in parser.rs (lines 79/124/192/286) are Agent C's, not mine.
